# Deterministic raw-record preprocessing

The authoritative `ptm.preprocessing.v1` reference has moved to
[the manual](manual/reference/preprocessing.md). The
[host-pipeline explanation](manual/explanation/host-pipelines.md) distinguishes
portable preprocessing from Python-only connector transforms.

This compatibility page preserves the original public path.
